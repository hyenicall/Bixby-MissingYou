endpoints {
  action-endpoints {
    action-endpoint (SearchAnimal) {
      accepted-inputs (keyword, city, county, dateTimeExpression, adoption)
      local-endpoint (searchAnimal.js)
    }
       
    action-endpoint (OpenApp) {
      accepted-inputs (tel,loc)
      local-endpoint (OpenApp.js)
    }
  }

  
}
